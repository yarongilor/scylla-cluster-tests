test_duration: 200

stress_cmd: "cassandra-stress mixed cl=QUORUM duration=80m -schema 'replication(factor=3) compaction(strategy=LeveledCompactionStrategy)' -mode cql3 native -rate threads=6 -pop seq=1..2003004 -log interval=5"

n_db_nodes: 3
n_loaders: 1
n_monitor_nodes: 1

instance_type_db: 'i3.large'


user_prefix: 'longevity-tombstone-gc-modes'

# Table settings:
# gc_grace_seconds: 4 minutes
# TTL: 4 minutes
# TWCS time window size: 4 minutes
# propagation-delay: 4 minutes
#post_prepare_cql_cmds: ["ALTER TABLE scylla_bench.test with gc_grace_seconds = 240 and default_time_to_live = 240 and compaction = {'class':'TimeWindowCompactionStrategy', 'compaction_window_size': 20, 'compaction_window_unit': 'MINUTES'} and tombstone_gc = {'mode': 'disabled', 'propagation_delay_in_seconds':'240'};",
#                        "ALTER TABLE keyspace1.standard1 with gc_grace_seconds = 240 and default_time_to_live = 240 and tombstone_gc = {'mode': 'disabled', 'propagation_delay_in_seconds':'240'};"
#                       ]

post_prepare_cql_cmds: "ALTER TABLE keyspace1.standard1 with gc_grace_seconds = 240 and default_time_to_live = 240 and tombstone_gc = {'mode': 'disabled', 'propagation_delay_in_seconds':'240'};"
